import * as React from "react";
import { FirestoreQuery } from "../types";
export declare class FirestoreDocument extends React.Component<FirestoreQuery & {
    children: ({}: {
        path: string;
        value: any;
        isLoading: boolean;
    }) => React.ReactNode;
}> {
    render(): JSX.Element | null;
}
