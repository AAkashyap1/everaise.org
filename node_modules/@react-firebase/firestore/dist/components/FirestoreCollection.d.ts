import * as React from "react";
import { FirestoreQuery } from "../types";
export declare class FirestoreCollection extends React.Component<FirestoreQuery & {
    children: ({}: {
        path: string;
        value: any;
        ids: string[];
        isLoading: boolean;
    }) => React.ReactNode;
}> {
    render(): JSX.Element | null;
}
