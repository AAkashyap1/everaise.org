import * as React from "react";
export interface NewProps {
    [key: string]: any;
}
export declare type Renderable = ((props: NewProps) => any) | React.ReactNode | React.ReactNodeArray | React.Component<NewProps, any>;
export declare const renderAndAddProps: (renderable: Renderable, props: NewProps) => any;
export default renderAndAddProps;
