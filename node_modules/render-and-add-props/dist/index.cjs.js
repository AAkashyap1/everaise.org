'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');

var renderAndAddProps = function (renderable, props) {
    if (typeof renderable === "function") {
        return renderable(props);
    }
    else if (React.Children.count(renderable) > 0) {
        return React.Children.map(renderable, function (child) {
            return React.cloneElement(child, props);
        });
    }
    else if (React.isValidElement(renderable)) {
        return React.cloneElement(renderable, props);
    }
    return null;
};

exports.renderAndAddProps = renderAndAddProps;
exports.default = renderAndAddProps;
